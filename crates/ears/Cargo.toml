[package]
name = "ears"
description = "Audio capture, pitch detection, and onset detection for the AI Music Companion"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
# ringbuf = "0.4"       # Lock-free SPSC ring buffer — uncomment when needed
# cpal = "0.15"         # Cross-platform audio capture — uncomment when needed

[dev-dependencies]
